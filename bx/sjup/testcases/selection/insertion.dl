s("a", "a").
s("b", "default").

// Expected view

// v("b", "default").

v_prime("b", "default").
v_prime("c", "default").

// Expected source prime

// s_prime("a", "a").
// s_prime("b", "default").
// s_prime("c", "default").
