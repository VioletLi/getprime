CREATE s(
X STRING,
Y STRING;
PRIMARY KEY(X)
),

CREATE v(
X STRING,
Y STRING
);

ON SOURCE[s], VIEW v:

INSERT {X, Y} INTO s
WHEN NOT { X, 'd'} IN s && Y = 'd'
THEN INSERT {X, Y} INTO v,

DELETE {X, Y} FROM s
WHEN { X, Y} IN s && Y = 'd'
THEN DELETE {X, Y} FROM v.